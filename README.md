# [Re] Machine Learning Assisted Cervical Cancer Detection

**Reproduction and Evaluation Analysis**
Prakhar Sahu · Prakhar Sudele — 2026

This repository contains the code, figures, and article manuscript for a
reproduction study submitted to ReScience C.

**Original paper replicated:**
Mehmood, A., Maqsood, M., Bashir, M., & Shuyuan, Y. (2021).
Machine Learning Assisted Cervical Cancer Detection.
*Frontiers in Public Health*, 9, 788376.
https://doi.org/10.3389/fpubh.2021.788376

---

## What we found

The published confusion matrix reproduces exactly: 803 true negatives, 55
false negatives, and zero true or false positives across all 858 observations.
The reproduced classifier assigns every observation to the negative class, and
its 93.6% accuracy equals the majority-class baseline. The all-negative collapse is the modal outcome
across 30 random seeds, though qualitatively different collapse modes are also
observed. The behaviour holds across all 12 interpretations of the original's
ambiguous implementation choices. The original's Table 4 reports a true-positive rate of
100%, which is inconsistent with its own published confusion matrix.

---

## Repository structure

```
article/          ReScience C manuscript source (LaTeX)
  content.tex     Article body
  metadata.yaml   Submission metadata (authors, abstract, references)
  bibliography.bib
  article.tex     ReScience template main file (do not edit)
  rescience.cls   ReScience journal class
  *.png           Figure copies for LaTeX compilation
data/
  download_data.py  Downloads the UCI dataset (not redistributed here)
figures/          Figures as generated by the notebook
notebooks/        Original Colab notebook
requirements.txt  Python dependencies
```

---

## Data

The dataset is **not included** in this repository. Download it with:

```bash
python data/download_data.py
```

This fetches `risk_factors_cervical_cancer.csv` from the UCI Machine Learning
Repository (Fernandes et al., 2017):
https://archive.ics.uci.edu/dataset/383/cervical+cancer+risk+factors

---

## Installation

```bash
pip install -r requirements.txt
```

Tested on Python 3.10, CUDA 12.8, PyTorch 2.11.0, pandas 2.2.3.
The notebook was developed and run in Google Colab.

---

## Reproducing the results

The complete pipeline is in the notebook:

```
notebooks/Cervical_Cancer_Detection_Reproduction.ipynb
```

Open it in Google Colab (recommended) or Jupyter. Run all cells in order.
The notebook does not download data automatically. Run `data/download_data.py`
first; it saves the CSV to `data/risk_factors_cervical_cancer.csv`. In Colab,
upload that CSV to `/content/risk_factors_cervical_cancer.csv` after
downloading it locally.

**Expected runtime:** approximately 20–30 minutes on a Colab T4 GPU for the
full 30-seed stability sweep and 12-configuration grid.

---

## Figures and their sources

| Figure file | Notebook section | Description |
|---|---|---|
| `figures/rf_importance.png` | Feature Selection | Random Forest importance ranking (Section 2.3) |
| `figures/loss_trajectory.png` | Model Training | Training/validation loss, primary run seed 42 (Section 2.4) |
| `figures/seed_sweep.png` | Stability Analysis | Test accuracy across 30 seeds (Section 3.4) |
| `figures/confusion_matrix.png` | Results | Confusion matrices by partition, primary run (Section 3.1) |
| `figures/probability_distribution.png` | Ranking Analysis | Predicted probability distribution by class (Section 3.6) |

All figures are regenerated by the notebook.

---

## Compiling the article

Requires XeLaTeX and Python 3. From the `article/` directory:

```bash
cd article
python yaml-to-latex.py metadata.yaml
xelatex article.tex
bibtex article
xelatex article.tex
xelatex article.tex
```

Or simply:

```bash
cd article && make
```

---

## License

Code: MIT — see [LICENSE](LICENSE)
Article text (`article/`): CC BY 4.0
ReScience template files (`rescience.cls`, etc.): GPL-3.0
